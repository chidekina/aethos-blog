---
context: default
phase: none
status: paused
work_in_flight: false
last_updated: 2026-09-03T10:05:00Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

- [ ] CONSTRAINT: **`main` is protected, `gate` required, admins included.**
      Direct push refused, prose included. Branch + PR for everything.
      Lift deliberately with:
      `gh api -X DELETE repos/chidekina/aethos-blog/branches/main/protection/enforce_admins`

- [ ] CONSTRAINT: **Shell aliases corrupt command substitution.** `cat` is `bat`,
      `ls` is `eza`. Use `command ls` / `command cat` inside `$( )`.

- [ ] CONSTRAINT: **`cd X && cmd` is silently blocked in this harness.**
      Hit this session. Use absolute paths or `git -C`, never a `cd` prefix.

- [ ] CONSTRAINT: **`fetch-news.mjs` takes paths from ENV VARS, not flags.**
      `NEWS_CONFIG`, `NEWS_SEEN`, `NEWS_POSTS_DIR`, `NEWS_DIGEST_LOG`.
      Since 814471f an unknown flag exits 2, so this can no longer fail silently.

**Do not proceed until all four boxes are checked.**

## Critical Anti-Patterns (carried forward, all hit again this session)

| Pattern | This session's instance |
|---|---|
| Instrument answering about the wrong state | `--config` was ignored: a "clean sweep" confirmed 9 live feeds while 38 candidates were never fetched |
| A responding endpoint read as a working one | `/api/tags` answered instantly while `/api/generate` hung for 600s — same false-green as `pg_isready` |
| Exit code read as effect | background notification said "exit code 0" for a run whose real status was `124` (timeout). ~18th occurrence on this machine |
| Zero without a control | every feed probe and every grep here carried a positive/negative control; the 404 control caught the `--config` defect |
| A mutation that misses its target | the first mutation run timed out and **left the script mutated**; caught by diffing, not assumed |

<current_state>
Working tree clean except untracked `.planning/reports/`. Local `main` == `origin/main`
at **814471f**. Two PRs merged this session (#9, #10). CI green, production deploying
from `main` on push.

**The weekly digest automation is VERIFIED WORKING**, end to end, under the real cron
environment (`env -i PATH=/usr/bin:/bin`):

    52 seconds · RESULT=ok · 2 drafts written · items=8
    fetched 2767 · within 8d 150 · unseen 150 · scored 141 · shortlist 8

Verified with `NEWS_SEEN`/`NEWS_POSTS_DIR` redirected to a temp dir, because
`seen.json` is TRACKED and a real run would burn the links Monday needs.
</current_state>

<completed_work>
**PR #9 (`52d8b75`) — digest expanded from 9 to 34 feeds.**
Five research agents fetched every candidate; all 38 were then re-probed through
this repo's own parser, each run carrying a negative control that must 404.
`docs/FEED-CANDIDATES.md` records every candidate, every rejection with its reason,
and the dead ends worth not re-researching.

**PR #10 (`814471f`) — three intake controls + the two feeds they were blocking.**
- unknown flag -> exit 2 (was silently ignored)
- `maxPerSource` global + per-source `maxPerDigest` (cap existed but was hardcoded at 3)
- per-source `timeoutMs`
- TabNews added at `maxPerDigest: 1`; James Bach at `timeoutMs: 60000`
- **Found in review of #10:** `Number("three")` is NaN and `n >= NaN` is always
  false, so a typo'd cap silently removed the cap entirely — the exact firehose
  the change existed to prevent. Now validated as positive integers, exit 2 on bad.

Suite grew 22 -> 41 assertions, all mutation-verified with disjoint failure sets.
</completed_work>

<remaining_work>
1. **Wrapper has no timeout of its own.** Cron kills nothing, so a wedged Ollama
   leaves the job hanging forever with only `START` in the log. The handoff's own
   rule says "no log line at all means the job never ran" — there is now a THIRD
   state that looks identical: it ran and hung. Add a timeout with an explicit
   `RESULT=timeout` line. **This is the top item.**

2. **`maxItems` is still 8 while 133 candidates now clear `minScore`.** The digest
   is heavily oversubscribed after the expansion. Not a defect, but the number
   that should drive whether 8 is still the right size.

3. Client-side search across posts (pre-existing candidate, not started).

4. Review Monday's first cron-produced digest before publishing.
</remaining_work>

<decisions_made>
- **n8n is NOT the answer, and this was measured, not assumed.**
  `n8n.srv915988.hstgr.cloud` returns 404 on `/`, `/healthz`, `/api/v1`, `/rest/login`
  (control: github.com -> 200). It was deliberately removed 2026-08-20 per D-90-05
  for exactly this; see `~/projetos/.claude/worktrees/.../scripts/n8n/REMOVAL-RECORD.md`.
  Re-probed rather than trusting the record — an inherited objection is still a claim —
  and the claim holds.
- **The fragile dependency is INFERENCE, not scheduling.** Cron works; node resolves
  via nvm correctly under `env -i`. What broke mid-session was Ollama.
- Digest still writes `draft: true` only. The local model mangles proper nouns —
  observed this run: "Terminal-Bench-Science 0.1" became "Benchmark de Ciência do
  Terminal 0,1" in the PT draft. Review is where the value is added.
</decisions_made>

<blockers>
None blocking. One OPEN QUESTION the operator has not answered:

**Should the digest move off local inference?** Raised, not decided. GitHub Actions
`schedule` + a paid API (16 calls/week) would remove the machine dependency entirely.
It is an architectural change, so `/impact --adr` comes first per the standing rule.
Recorded as a question, not a plan — a decision by omission looks like "preserved"
and behaves like "discarded".
</blockers>

## Infrastructure State

- **Cron**: `0 8 * * 1 .../scripts/news/run-digest.sh`. Verified this session under
  `env -i PATH=/usr/bin:/bin`: the wrapper resolves nvm node (`v24.11.1`) correctly.
  Log at `scripts/news/digest.log` (gitignored), still absent — the job has not yet
  fired on its own schedule.
- **Ollama**: `llama3.2:3b` at `localhost:11434`. **Went wedged mid-session** — the
  runner sat at 0.0% CPU for 51 minutes while every `/api/generate` timed out at
  600s, cold and warm alike; embeddings on the resident `nomic-embed-text` answered
  instantly throughout. It cleared on its own; a later call answered in 9s and the
  full digest then ran in 52s. **Transient, and nothing detects or reports it.**
  Diagnose with a real generate call, never `/api/tags`.
- **Feeds**: 36 sources, 36/36 reachable. `seen.json` is TRACKED — redirect
  `NEWS_SEEN` when testing or you burn Monday's material.
- **Suites**: news 41 · cron wrapper 14 · recommendations 19.

<context>
The session's through-line: a mechanism that exists is not a mechanism that runs,
and every defect found came from USING the thing rather than reading it. The feed
research was verified by re-probing through the repo's own parser; the intake
controls were found by a reviewer and confirmed by execution; and the automation
question was only answered by running the wrapper the way cron runs it — which is
how both the Ollama wedge and the missing wrapper timeout surfaced. Two of the
defects were in code written minutes earlier in this same session.
</context>

<next_action>
Confirm state rather than trusting this file:

```bash
git -C . status --porcelain && git -C . log --oneline -1
crontab -l | grep aethos-blog
tail -20 scripts/news/digest.log 2>/dev/null || echo "(no cron run yet)"
# Ollama liveness — a real generate, never /api/tags:
curl -sf -m30 http://localhost:11434/api/generate -H 'content-type: application/json' \
  -d '{"model":"llama3.2:3b","prompt":"hi","stream":false}' -o /dev/null; echo "exit=$?"
```

Then item 1 of remaining_work: the wrapper timeout. Any change needs a branch and a PR.
</next_action>
