---
context: default
phase: none
status: paused
work_in_flight: false
last_updated: 2026-09-04T21:00:00Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

- [ ] CONSTRAINT: **`main` is protected — but `enforce_admins` is OFF since 2026-09-04.**
      Read back from the API after the change: `enforce_admins: false`, contexts
      still `["gate"]`, force-push and deletion still refused.
      **Prose may now go straight to `main`** (the docs-only carve-out works again).
      **Code still needs a PR** — but that is now held only by the LOCAL
      `~/.git-hooks/pre-commit` (11 assertions, both ends), which `--no-verify`
      bypasses and GitHub will no longer catch. The rule, not the server.
      Restore with:
      `gh api -X PUT repos/chidekina/aethos-blog/branches/main/protection/enforce_admins`

- [ ] CONSTRAINT: **`bun --cwd DIR run <script>` runs NOTHING and exits 0.**
      It prints the script list. The working form is `bun run --cwd DIR <script>`.
      Tell them apart by the line naming the volume of work (`Result (14 files)`),
      never by the exit code. Full note in `CLAUDE.md`.

- [ ] CONSTRAINT: **`cd X && cmd` is silently blocked in this harness.**
      Exit 1, no message, reads as the command failing. Absolute paths or `git -C`.

- [ ] CONSTRAINT: **Shell aliases corrupt command substitution.** `cat` is `bat`,
      `ls` is `eza`. Use `command ls` / `command cat` inside `$( )`.

- [ ] CONSTRAINT: **`fetch-news.mjs` takes paths from ENV VARS, not flags.**
      `NEWS_CONFIG`, `NEWS_SEEN`, `NEWS_POSTS_DIR`, `NEWS_DIGEST_LOG`.
      `--dry-run` is safe: it exits before any write, so `seen.json` is untouched.

**Do not proceed until all five boxes are checked.**

## Critical Anti-Patterns — four of these were hit THIS session

| Pattern | This session's instance |
|---|---|
| A number in prose outliving its meaning | "5 of 8 rewritten" was in 3 docs including an ADR that reasoned from it. Real: **7 of 7**. Worse — I fed it to research briefs, and two agents built a plan on it |
| A control reading the render, not the predicate | `pre-task.sh` caps output at 2 warnings; my negative control read the truncated output and reported the fix had gone deaf. It had not |
| The "before" that was captured after | My pre-fix backup was copied AFTER applying the fix, so before/after read 0 → 0. The real before came from `git show HEAD:<file>` |
| Exit code read as effect | `bun --cwd` returned 0 having run nothing |
| Zero without a control | every probe, grep and URL check here carried a positive and a negative control |

<current_state>
Working tree **clean**. Local `main` == `origin/main` at **b44387a**.
**Eight PRs merged this session in aethos-blog (#13-#20)**, plus **`claude-config#69`**.
CI green on every one; production driven end to end after #14 and re-smoked after #18.

Nothing is in flight.
</current_state>

<completed_work>
**#13 sizing · #14 search · #15 ADR-001 raised · #16 handoff · #17 SHA drift**
(see git log for these; #14 is the only one that changed the site)

**#18 (`54a616d`) — `.planning/reports/` gitignored.** They were untracked and
unignored, appearing in every `git status`. Ignored because a session report is
*derived* — recomputable from `git log`. The control that mattered: `.continue-here.md`
and `HANDOFF.json` must stay visible to git, and a pattern one segment broader
(`.planning/`) would have swallowed them silently.

**#19 (`ac33a69`) — ADR-001 ACCEPTED: stay on local Ollama.** Operator decision.
The rationale changed between proposing and accepting, and that is the substance:
the first edition survives in git as a labelled pair (`d44b785` draft vs `e8d6510`
published), so it was recomputed rather than remembered.

| | EN | PT |
|---|---:|---:|
| items the model produced | 8 | 8 |
| URLs the human kept | **7** | 7 |
| model sentences surviving verbatim | **0 of 7** | **0 of 7** |

**Selection works and uses no LLM** — keyword scoring in `score()`, before
`summarizeEn` is ever called. A hosted API would buy quality in the step whose
output is 100% discarded.

**#20 (`b44387a`) — research pass, `docs/DIGEST-EVAL.md`.** Four angles, every
claim carrying a fetched URL, spot-checked with negative controls. The headline is
a collision: two independent passes proposed harvesting "free labels" from
rewritten-vs-kept, which is a **constant column** at 7 of 7 — κ undefined. They
inherited the wrong number from our own prose.

**`claude-config#69` (`895b236`)** — `pre-task.sh` warned "No ARCHITECTURE.md found"
about repos that have `docs/ARCHITECTURE.md`. Measured: 2 false positives of 79
dirs with a `src/` (not the whole park — I overstated it first). Fixed, and the
hook had no test at all; added one, 5 arms, two mutations with disjoint failure sets.
</completed_work>

<remaining_work>
1. **Review the first cron-produced digest.** Cron fires **Monday 2026-09-07 08:00**;
   it has never fired on its own schedule and `scripts/news/digest.log` is still absent.
   Gated on the clock, not on work.
   🔴 Read the `RESULT=` line, never the exit code — and a log with NO `RESULT=` line
   is a third state (ran, never finished), not a zero in either column.

2. **Entity/number preservation check** — item 1 of `docs/DIGEST-EVAL.md`. Deterministic,
   ~30 lines, zero dependencies, no labels needed. Targets the one failure actually
   observed: `Terminal-Bench-Science 0.1` became `Benchmark de Ciência do Terminal 0,1`
   in the PT draft. Offered to the operator, not started.

3. **Persist the draft→published pair per edition** — item 2 of the same doc. Edition 1
   is recoverable only by git archaeology and only because the draft happened to be
   committed. Nothing captures it deliberately, so no eval set can accumulate.

4. **ADR-001 follow-up, deliberately open: does the summarization step earn its place?**
   Measured contribution to published prose is zero, but it may pay for itself as review
   scaffolding. `--no-llm` already exists and falls back to the raw feed excerpt — one
   edition run that way answers it.

5. Optional, one line: add `huyenchip.com/feed.xml` to `sources.json`. Verified through
   this repo's own parser (10 items, negative control 404). Three of the four blogs the
   operator wanted to follow are already feeds; this is the fourth.
</remaining_work>

<decisions_made>
- **Stay on local Ollama (ADR-001, Accepted).** Operator decision, and the measurement
  strengthened it: paying for better summaries optimizes the discarded step.
- **Do not build a faithfulness judge yet.** MiniCheck is genuinely SOTA for this shape
  but is 4.7 GB with no licence tag on the 7B, and item 4 above may delete the step it
  would judge.
- **promptfoo not adopted.** Real, MIT, maintained, Ollama first-class — its value is a
  prompt×model matrix we do not have. Revisit the day we compare two prompts.
- **Playwright is not a CI dependency.** The build-time search-index gate guards PRs;
  the browser pass is manual when the homepage script changes.
- **`.planning/reports/` ignored, not tracked.** Derived artifacts; both conventions
  exist across ~/projetos (3 ignore, ~9 track) so this is a choice, revertible.
</decisions_made>

<blockers>
None. Item 1 waits for Monday, which is a clock, not a blocker.
</blockers>

## Infrastructure State

- **Cron**: `0 8 * * 1 .../scripts/news/run-digest.sh`. Wrapper timeout shipped (#12):
  `RESULT=timeout` on 124, `WARN` and fail-OPEN when coreutils `timeout` is missing.
- **Ollama**: `llama3.2:3b` at `localhost:11434`. Wedge is transient and *detected*,
  not prevented. Diagnose with a real generate call, never `/api/tags`.
- **Feeds**: 36 sources. `seen.json` is TRACKED — redirect `NEWS_SEEN` when testing.
- **Search**: live in production, `dist/search-index.json` 224 KB, 42 posts indexed.
- **Suites**: news 46 · cron wrapper 20 · recommendations 19 · in-CI search-index and
  draft-leak assertions.
- **`~/.claude`**: on `main` with the `pre-task.sh` fix live. 🔴 `claude-config#68`
  (`fix/patch-survival-line-attribution`) is OPEN and therefore **out of the air** —
  `~/.claude` is live config, so an unmerged branch is an improvement not running.

<context>
Two through-lines this session, and they are the same one from opposite ends.

**A note about state is not state.** The opening checkpoint named an item that had
already shipped in #12, and the "5 of 8" number was wrong in three documents including
the ADR that reasoned from it. Nothing re-measures a file that no longer matches
the world.

**And a wrong number propagates outward, not just forward.** I put "5 of 8" into
four research briefs before measuring it. Two agents built a recommendation on it and
reasoned correctly from a false premise — the plan they produced is void, and one of
them shipped the very control that catches it. Measure before briefing, not after.
</context>

<next_action>
Confirm state rather than trusting this file:

```bash
R=/home/hidekina/projetos/aethos/aethos-ideas/aethos-blog
git -C $R status --porcelain && git -C $R log --oneline -1
crontab -l | grep aethos-blog
tail -20 $R/scripts/news/digest.log 2>/dev/null || echo "(no cron run yet)"
curl -sf -m30 http://localhost:11434/api/generate -H 'content-type: application/json' \
  -d '{"model":"llama3.2:3b","prompt":"hi","stream":false}' -o /dev/null; echo "exit=$?"
```

If Monday has passed: review each draft line against its linked source before setting
`draft: false`. Measured on edition 1, **zero** model sentences survived into the
published post — assume you are rewriting, not proofreading.

Otherwise the cheapest real work is item 2 (the entity check): it needs no labels,
no model and no decision from item 4.
</next_action>
