---
context: default
phase: none
status: paused
work_in_flight: false
last_updated: 2026-09-04T19:40:00Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

- [ ] CONSTRAINT: **`main` is protected, `gate` required, admins included.**
      Re-read from the API this session: `enforce_admins: true`, contexts `["gate"]`.
      Direct push refused, prose included. Branch + PR for everything.
      Lift deliberately with:
      `gh api -X DELETE repos/chidekina/aethos-blog/branches/main/protection/enforce_admins`

- [ ] CONSTRAINT: **Shell aliases corrupt command substitution.** `cat` is `bat`,
      `ls` is `eza`. Use `command ls` / `command cat` inside `$( )`.

- [ ] CONSTRAINT: **`cd X && cmd` is silently blocked in this harness.**
      Hit again on the FIRST command of this session — exit 1, no message, and it
      reads as the command having failed. Use absolute paths or `git -C`.

- [ ] CONSTRAINT: **`bun --cwd DIR run <script>` does NOT run the script.**
      Found this session. It prints the script LIST and exits **0** — a false green
      that looked exactly like a passing typecheck. The working form is
      `bun run --cwd DIR <script>`. Tell them apart by the output: the real run
      names files (`Result (14 files)`), the fake one lists `$ bun run build`.

- [ ] CONSTRAINT: **`fetch-news.mjs` takes paths from ENV VARS, not flags.**
      `NEWS_CONFIG`, `NEWS_SEEN`, `NEWS_POSTS_DIR`, `NEWS_DIGEST_LOG`.
      Since 814471f an unknown flag exits 2, so this can no longer fail silently.

**Do not proceed until all five boxes are checked.**

## Critical Anti-Patterns (carried forward; the first three hit again this session)

| Pattern | This session's instance |
|---|---|
| Exit code read as effect | `bun --cwd` returned 0 having run nothing |
| A stale note read as current state | the previous checkpoint named the wrapper timeout as "the top item"; PR #12 had already shipped it |
| Zero without a control | every grep, feed probe and index assertion here carried a positive control; the search-index gate fails if `checked == 0` |
| A number in prose outliving its meaning | "133 clear minScore" implied a starved digest; the reachable pool is 42 |
| Instrument answering about the wrong state | `contract-validate.py` run from outside the repo — relative globs would scan the wrong tree |

<current_state>
Working tree clean except untracked `.planning/reports/` (session reports from earlier
dates, never committed). Local `main` == `origin/main`. **Four** PRs merged this session:
**#13, #14, #15, #16**. CI green on each; production driven end to end after #14.

🔴 Do not trust a SHA written in a handoff — including the one in `HANDOFF.json`. A
handoff cannot record the SHA of its own merge, so the field is correct when written and
stale one commit later, by construction. `git rev-parse HEAD` is the answer.

The four remaining_work items from the previous handoff are **closed or gated**:

1. ~~Wrapper timeout~~ — was ALREADY DONE in PR #12 before this session started.
   The checkpoint was stale. Verified in `run-digest.sh:88-106`, not assumed.
2. Digest sizing — **closed by measurement, no knob changed** (PR #13).
3. Client-side search — **shipped and live** (PR #14).
4. Local-inference question — **ADR-001 written, decision deliberately still open** (PR #15).
</current_state>

<completed_work>
**PR #13 (`76ef888`) — the sizing question, answered by measuring.**
The handoff's "133 candidates clear minScore" implied starvation. Live dry-run:
140 clear `minScore`, but `maxPerSource: 3` cuts the reachable pool to **42**.
The choice is 8 of 42, not 8 of 140. Lowering `maxPerSource` to 2 was tried on
the same run and REJECTED — distinct sources stayed 5 either way and it traded a
score-17 item for a score-12. Both knobs unchanged; `docs/NEWS-PIPELINE.md` now
carries the two recompute commands, the second being a control that catches a
`NEWS_CONFIG` that was ignored.

**PR #14 (`3cd5835`) — client-side full-text search.**
Metadata-only search was built first and thrown away: of 20 plausible terms, **9
appear only in bodies** (`typescript`, `deploy`, `migration`, `cache`, `webhook`,
`neon`, `rss`, `stripe`, `benchmark`). Bodies are indexed via a build-time
endpoint into `dist/search-index.json`, fetched lazily on first focus — homepage
grew only 142010 → 145980 bytes; the 224 KB index is downloaded only by a searcher.
CI gate derives a body-only word per post and asserts it reached the index;
mutation-verified (deleting `toPlainText(post.body)` turns it red). 14 checks
driven in headless Chromium at 390px, then the SAME 14 re-run against production.

**PR #15 (`40cbd99`) — ADR-001, the inference question.**
Created `docs/adr/` (the repo had none). Records the measured facts — 16 calls/week,
blast radius of 3 code files, zero `src/` involvement — and the three consequences
a cost comparison hides: it breaks CONTRACT invariant 5, `main` protection forces a
bot-authored PR path, and Actions `schedule` skips silently. Recommends waiting for
evidence. No pricing quoted; a price in prose rots.
</completed_work>

<remaining_work>
1. **Review the first cron-produced digest.** Cron fires Monday **2026-09-07 08:00**;
   it has never yet fired on its own schedule. `scripts/news/digest.log` is still
   absent. This is the top item and it is gated on time, not on work.
   🔴 Read the log's `RESULT=` line, never the exit code — and remember a log with
   NO `RESULT=` line is a third state (ran and never finished), not a zero.

2. **Then close ADR-001** using the evidence from runs 1-3. The ADR names the
   commands and their control.

3. Nothing else is open. The digest sizing and search items are done.
</remaining_work>

<decisions_made>
- **Digest sizing: change nothing.** The alarming number was not the binding one.
  Recorded with the measurement so it is not re-proposed from the same intuition.
- **Search indexes full bodies, in a separate lazily-fetched file.** Metadata-only
  is free and measurably not a search; inlining bodies would triple the homepage.
- **Playwright is NOT a CI dependency.** The build-time index gate guards every PR;
  the browser pass is manual, run when the homepage script changes. Flagged as a
  trade rather than hidden — revisit if that script grows.
- **ADR-001 stays Proposed.** Writing "open" down is the point; the alternative is
  a decision by omission, which looks like "preserved" and behaves like "discarded".
</decisions_made>

<blockers>
None. Item 1 is waiting for Monday, which is not a blocker but a clock.
</blockers>

## Infrastructure State

- **Cron**: `0 8 * * 1 .../scripts/news/run-digest.sh`. Wrapper timeout shipped
  (#12): `RESULT=timeout` on 124, and a `WARN` when coreutils `timeout` is missing
  — it fails OPEN there, deliberately.
- **Ollama**: `llama3.2:3b` at `localhost:11434`. Wedge is transient and now
  *detected*, not prevented. Diagnose with a real generate call, never `/api/tags`.
- **Feeds**: 36 sources. `seen.json` is TRACKED — redirect `NEWS_SEEN` when testing.
  Dry-run does NOT touch it (it exits before any write), so `--dry-run` is safe.
- **Search**: `dist/search-index.json`, 224 KB, 42 posts. Live in production.
- **Suites**: news 46 · cron wrapper 20 · recommendations 19 · plus the in-CI
  search-index and draft-leak assertions.

<context>
The session's through-line, and it is the same one as last time from the other end:
**a note about state is not state.** The previous checkpoint named an item that had
already shipped, and the handoff carried a number whose meaning had changed. Both
read as current because nothing re-measures a file that no longer matches the world.
Every item here was therefore re-measured before being worked, and the one that
survived contact (`maxItems`) turned out to need no change at all — the finding was
that the number in the note was the wrong number, not that the config was wrong.
</context>

<next_action>
Confirm state rather than trusting this file:

```bash
R=/home/hidekina/projetos/aethos/aethos-ideas/aethos-blog
git -C $R status --porcelain && git -C $R log --oneline -1
crontab -l | grep aethos-blog
tail -20 $R/scripts/news/digest.log 2>/dev/null || echo "(no cron run yet)"
# Ollama liveness — a real generate, never /api/tags:
curl -sf -m30 http://localhost:11434/api/generate -H 'content-type: application/json' \
  -d '{"model":"llama3.2:3b","prompt":"hi","stream":false}' -o /dev/null; echo "exit=$?"
```

If Monday has passed: review the digest drafts line by line against their sources
before setting `draft: false`. The first edition needed 5 rewrites and 1 removal
out of 8 — that rate is the reason the generator never publishes.
