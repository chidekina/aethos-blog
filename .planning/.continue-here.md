---
context: default
phase: none
status: paused
work_in_flight: false
last_updated: 2026-09-04T21:10:00Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

- [ ] CONSTRAINT: **`main` is protected — but `enforce_admins` is OFF since 2026-09-04.**
      **Prose may go straight to `main`.** **Code still needs a PR**, held now only
      by the LOCAL `~/.git-hooks/pre-commit`, which `--no-verify` bypasses and
      GitHub no longer catches. The rule, not the server.
      Restore: `gh api -X PUT repos/chidekina/aethos-blog/branches/main/protection/enforce_admins`

- [ ] CONSTRAINT: **That local pre-commit BLOCKS a diff over 800 lines.** It
      rejected the first version of PR #22 at 1181 lines with "Mixed concerns:
      src(5), docs(3), infra(1)". It was right; the work split into four focused
      commits. Budget for the split rather than reaching for `--no-verify`.

- [ ] CONSTRAINT: **`bun --cwd DIR run <script>` runs NOTHING and exits 0.**
      Working form is `bun run --cwd DIR <script>`. Tell them apart by the line
      naming the volume (`Result (14 files)`, `46 page(s) built`), never the exit code.

- [ ] CONSTRAINT: **`cd X && cmd` is silently blocked in this harness.** Absolute
      paths or `git -C`.

- [ ] CONSTRAINT: **This shell is zsh, and `$var:path/...` is a PARAMETER MODIFIER.**
      `git show $c:src/content/...` silently became `d44b785-ai-dev.mdx` — the `:s/`
      form ran a substitution. Always brace: `git show "${c}:src/..."`.

- [ ] CONSTRAINT: **`fetch-news.mjs` takes paths from ENV VARS, not flags.**
      `NEWS_CONFIG`, `NEWS_SEEN`, `NEWS_POSTS_DIR`, **`NEWS_EDITIONS_DIR`** (new).
      A `--config` flag is silently ignored and probes the live sources.json.

**Do not proceed until all six boxes are checked.**

## What this session actually cost, and what it bought

| pattern | this session's instance |
|---|---|
| A fixture suite cannot report a false positive rate | check-entities read **23/0** on fixtures and **2 false positives in 8** on real edition-1 drafts, both `AI`→`IA` |
| A claim in prose that nothing re-measured | DIGEST-EVAL's motivating failure **never happened** — and it had already reached a module docstring and a test arm labelled "THE OBSERVED PRODUCTION FAILURE" |
| Source corrected, derivative left in circulation | NEWS-PIPELINE still said "five rewritten" after ADR-001 fixed it to 7 of 7 — in the file an operator reads before reviewing |
| A control reading the render, not the predicate | `Array.join` renders `null` as `""`, so my ARM 1 negative control could not tell `null` from `strong` |
| A mutation that misses its target reads as robustness | M1's anchor drifted after a refactor; the harness reported **ANCHOR MISSED** instead of green. The guard worked |
| A suite's side effects are invisible to the suite | `fetch-news.test.sh` wrote fixture edition records into the tracked tree until `NEWS_EDITIONS_DIR` was redirected |
| Exit code read as effect | my own Ollama probe printed `gen exit=0` — that was `head`'s status; the body was empty |

<current_state>
Working tree **clean**. Local `main` == `origin/main` at **53a57e9**.
**PRs #22 and #23 merged** (rebase and squash), CI green on both, #23 re-gated
against the new base after #22 landed. Production smoked: site 200,
`search-index.json` 200.

Nothing is in flight.
</current_state>

<completed_work>
**#22 — the eval pair (DIGEST-EVAL items 1-3), four focused commits.**

- `scripts/news/check-entities.mjs` — deterministic entity/number preservation.
  **Three lanes, where the doc specified two.** `EN→source` and `PT→source+EN`
  see INVENTION; the third, `EN→PT`, sees DELETION, and is the only direction
  that could catch a translated identifier. 26 assertions, 4 disjoint mutations.
- `scripts/news/editions/<date>.json` — the draft half, written by
  `fetch-news.mjs` whenever a draft reaches disk, carrying the **exact 700-char
  excerpt the prompt showed the model**. `capture-published.mjs` fills the
  published half, keyed by link. 15 assertions, 3 disjoint mutations.
- The entity check runs inline at draft time as advisory `ENTITY_CHECK` lines.
  Advisory on purpose; the point is that it is **invoked**.
- CI runs both suites **and both mutation harnesses**.

**#23 — Chip Huyen added to `sources.json`.** Verified through this repo's own
parser with a 404 negative control in the same run. 37/37 feeds reachable.

**Two numbers retracted, both by measuring the file instead of reading about it:**

| claim | measured |
|---|---|
| `Terminal-Bench-Science 0.1` → `Benchmark de Ciência do Terminal 0,1` in the PT draft | **did not happen.** `d44b785` line 16 keeps the identifier; only the separator changed, which is correct pt-BR. **Edition 1 has zero entity manglings** |
| review rewrote "five" of eight | **7 kept, 1 cut, 0 verbatim** — matches ADR-001, refutes NEWS-PIPELINE |
</completed_work>

<remaining_work>
1. **Review the first cron-produced digest.** Cron fires **Monday 2026-09-07 08:00**;
   it has never fired on its own schedule and `scripts/news/digest.log` is still
   absent. Gated on the clock, not on work. The only item left.
   🔴 Read the `RESULT=` line, never the exit code — and a log with NO `RESULT=`
   line is a third state (ran, never finished), not a zero in either column.
   🔴 **Ollama was wedged all session** and nothing fixed it: `/api/tags` answered
   instantly, `/api/generate` returned nothing in 140 s. If it is still wedged on
   Monday the run exits **2** and writes no draft — that is the guard working, not
   a quiet news week. Clear it before Monday or expect no digest.
   New this week: the run also writes `scripts/news/editions/2026-09-07.json` and
   prints `ENTITY_CHECK` lines. After publishing, run
   `node scripts/news/capture-published.mjs scripts/news/editions/2026-09-07.json`
   — it refuses while `draft: true`, which is the point.

2. **Decide ADR-001's open follow-up.** Now a choice between two measured options
   rather than an argument — see `docs/DIGEST-EVAL.md` §3. 🔴 Do not decide it
   against the current fallback: all 8 `--no-llm` lines end mid-word because the
   fallback is `slice(0,220) + '…'`, and cutting at a word boundary would shrink
   the gap for a few lines of code. That third option is unbuilt.
</remaining_work>

<decisions_made>
- **`TRANSLATION_EQUIVALENTS` carries only measured entries.** One today: `AI`→`IA`.
  An allowlist grown by guessing is a place to hide real failures.
- **The entity check is advisory, never blocking.** These are drafts and a human
  reads every line; a blocking gate would stop an edition over a token the
  reviewer was about to fix.
- **`capture-published` refuses while `draft: true`** (exit 1) and treats zero
  matched links as exit 2. Recording the model's output as the human's would make
  every later measurement the model grading itself.
- **`scripts/news/editions/` is tracked.** An eval set on one machine is not one.
- **#22 merged by rebase, not squash** — squashing would collapse it back into the
  1181-line commit the pre-commit gate rejected.
</decisions_made>

<blockers>
None. Item 1 waits for Monday, which is a clock. Ollama being wedged is a real
risk to Monday's run and is named above rather than left to be discovered.
</blockers>

<next_action>
Confirm state rather than trusting this file:

```bash
R=/home/hidekina/projetos/aethos/aethos-ideas/aethos-blog
git -C $R status --porcelain && git -C $R log --oneline -1
crontab -l | grep aethos-blog
tail -20 $R/scripts/news/digest.log 2>/dev/null || echo "(no cron run yet)"
# Ollama: the CATALOGUE answering is not generation working. Probe generation,
# capture the status WITHOUT a pipe, and read the body.
OUT=$(timeout 60 curl -s -m50 http://localhost:11434/api/generate \
  -H 'content-type: application/json' \
  -d '{"model":"llama3.2:3b","prompt":"hi","stream":false}'); echo "status=$? body=${OUT:0:80}"
```

If Monday has passed: review each draft line against its linked source before
setting `draft: false`, then run `capture-published.mjs`. Measured on edition 1,
**zero** model sentences survived into the published post — assume you are
rewriting, not proofreading.
</next_action>
